Q：SPA の特徴を通常の Web ページと比較して説明

SPA（Single Page Application）は、1 つのページだけで構成される Web アプリです。
初回にページ全体を読み込み、その後の画面更新は JavaScript によりページの一部だけを書き換えます。

■ 通常の Web ページとの違い

通常の Web ページ
ページを移動するたびに新しいページをサーバーから読み込む。

SPA
ページ再読み込みを行わず、必要なデータだけを非同期通信で取得し、画面の一部だけ更新する。
→ デスクトップアプリのように高速でスムーズな操作性になる。
