# 演習1

・HTTPメソッドとは「クライアント」から「サーバー」に対して発行する命令。
・「GET /buf.php HTTP/1.1」のように、メソッド、パス､「プロトコル情報」で構成される。
・ブラウザからページを要求する基本的な命令は「GET」を使う。

**語群：**
クッキー情報 / PUT / プロトコル情報 / クライアント / セッション情報 /
サーバー / CONNECT / クエリ情報 / GET
